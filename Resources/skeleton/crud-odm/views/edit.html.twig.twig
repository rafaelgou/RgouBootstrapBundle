{{ "{% form_theme edit_form 'RgouBootstrapBundle:Form:widgets.html.twig' %}" }}

{{ "{% extends '" ~ rgou_bootstrap.template ~ "' %}" }}

{{ '{% block headline %}' }}{{ "{{ '" ~ entity ~ "'|trans }} {{ 'edit'|trans }}" }}{{ '{% endblock headline %}' }}

{{ "{% block " ~ rgou_bootstrap.block ~ " %}" }}

<form class="form-horizontal" action="{{ "{{ path('"~ route_name_prefix ~"_update', { 'id': document.id }) }}" }}" method="post" {{ "{{ form_enctype(edit_form) }}" }}>
    {{ "{{ form_widget(edit_form) }}" }}
    <p>
        <button class="btn" type="submit">
            <i class="icon-pencil"></i>
            {{ "{{ 'edit'|trans }}" }}
        </button>
    </p>
</form>

{% set hide_edit, hide_delete = true, false %}
{% include 'views/others/record_actions.html.twig.twig' %}

{{ "{% endblock %}" }}