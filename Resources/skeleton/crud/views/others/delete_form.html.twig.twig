{{ "{{ form_start(delete_form, {'action': path('" ~  route_name_prefix ~ "_delete', { 'id': record.id }), 'method': 'POST', 'attr': { 'id': 'delete_form'} }) }}" }}
{{ "{{ form_widget(delete_form) }}" }}
{{ "{{ form_end(delete_form) }}" }}

<div class="modal fade" id="delete_modal" tabindex="-1" role="dialog" aria-labelledby="{{ 'Delete'|trans({},'rgoubootstrap') }}" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
                <h4 class="modal-title">{{ "{{ 'Delete'|trans({},'rgoubootstrap') }}" }} {{ entity }}</h4>
            </div>
            <div class="modal-body">
                <p>{{ "{{ 'Are you sure you want delete this record?'|trans({},'rgoubootstrap') }}" }}</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default" data-dismiss="modal">{{ "{{ 'Cancel'|trans({},'rgoubootstrap') }}" }}</button>
                <button type="button" class="btn btn-primary" id="delete_button_yes">{{ "{{ 'Delete'|trans({},'rgoubootstrap') }}" }}</button>
            </div>
        </div>
    </div>
</div>
<hr/>
